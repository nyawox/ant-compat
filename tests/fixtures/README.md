`tool_descriptions.yaml` - raw descriptions of tools from cc in the form of a YAML file. gitignored for legal reasons. extract from `cli.mjs` if needed.
  - "multiedit"
  - "edit"
  - "write"

`system_prompt.yaml`
  - "prompt"
